version: '2'
services:
  yahoo-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/yahoo/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=yahoo
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
